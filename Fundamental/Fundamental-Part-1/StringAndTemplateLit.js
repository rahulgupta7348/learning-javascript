/**A template literal (also called a template string) is a modern way to create and work with strings in JavaScript. It uses backticks (`) instead of quotes (' or ") and allows you to:

1. Embed expressions using ${}.
2. Create multi-line strings without special characters.
3. Add dynamic content seamlessly. */

/*
const firstName = 'Ram';
const birthYear = 1995;
const now = 2025;
const job = "Singer"; 


const ramInfo = "I'm "+firstName+", a "+(now-birthYear)+" year old "+ job+ "!";
console.log(ramInfo);

const ramInfoNew = `I'm ${firstName} a ${now-birthYear} old year ${job}!`;
console.log(ramInfoNew);
*/

// console.log("String with \n\
// multiple \n\
// lines");

// console.log(`String with 
// template 
// literals`);