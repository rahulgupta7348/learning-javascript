/*
1. Expression
An expression is a piece of code that produces a value when evaluated. It can be as simple as a number or a string or more complex like a function call or an arithmetic operation.

 Key Point: Expressions always return a value.

example:
5 + 3  // Evaluates to 8
"x" + "y"  // Evaluates to "xy"
true && false  // Evaluates to false
Math.max(10, 20)  // Evaluates to 20

2. Statement
A statement is an instruction that performs an action. Statements do not necessarily return a value but instead tell the JavaScript engine what to do.

🔹 Key Point: Statements do something, but they may not always produce a value.

example:
let x = 10;  // Variable declaration statement
if (x > 5) { console.log("x is greater than 5"); }  // If statement
for (let i = 0; i < 3; i++) { console.log(i); }  // Loop statement

*/
